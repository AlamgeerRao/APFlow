# Backlog

Items raised by architectural decisions but not implemented at the time the
decision was made. Each entry links back to the decision doc that created it.
Remove an entry once its work package/task lands and note the commit/WP that
closed it.

| Item | Trigger | Priority | Raised by |
|---|---|---|---|
| ~~**WP-085 live verification not yet performed**~~ — GB Skips side resolved same day: `deploy-web`'s cascading-skip bug (see closed WP-078 item above) was found and fixed (`9992d8c`), then a real push (outside this session) confirmed `deploy-web` genuinely ran this time (GitHub Actions API: real "Deploy to Azure App Service" step, not just build/test), and the user confirmed live that `RECEIVED`/`PROCESSING`/`EXTRACTED` no longer appear in the GB Skips nav. **Still open:** no platform-default-tenant test/demo account exists at all — every CIAM account created so far (WP-064's `apflow-test-reviewer`/`apflow-test-approver`/demo pair) belongs to the GB Skips tenant (`tid` `641fc267-...`), so the platform-default side of the nav fix is still confirmed only via the passing unit test against the platform-default fixture's exact status set, not a real sign-in. | Provisioning a platform-default-tenant test account | Low — GB Skips side (the tenant actually in active use) is confirmed live; platform-default is a secondary, less urgent gap | WP-085 report, 2026-08-04 |
| **`NEEDS_REVIEW_FEBINA` role-gating fix not yet live-verified post-deploy** — `4b1ce3a` (pushed/deployed outside this session, confirmed via GitHub Actions API: both `deploy-api`/`deploy-web` ran and succeeded) added the three `NEEDS_REVIEW_FEBINA → *` resolution edges to `RoleGatedTransitions`, requiring `FINANCE_MANAGER`. The bug itself was confirmed live pre-fix (a real `AP_REVIEWER` token could execute the resolution transition); the fix is unit-tested (390/390 backend tests) and deployment is confirmed via CI job success, but the corrected live behavior itself (reviewer now gets `403 Approval.Unauthorized`, approver still succeeds) has not been re-tested against the deployed build. | Next session — re-run the same live ROPC-token check used to find the original bug, this time expecting a 403 for the reviewer | Medium — a real authorization gap was closed, but "closed" is confirmed by code/tests/deploy-success, not yet by a live re-test | This report, 2026-08-04 |
| **WP-025 LOW-1: nav shows 14 of GB Skips' 15 statuses — acceptance-criteria wording doesn't match actual, deliberate design** — `navConfig.ts`'s `buildInvoiceQueueLinks` filters out terminal statuses (a WP-014-era decision: one nav link per *actionable* status), so `ARCHIVED` (confirmed present and correct in the database, all 15 `StatusReferences` rows exist) never appears as a nav link — by design, unrelated to WP-075's fix (which correctly made the `workflow-template` *API* return all 15). `Sprint1-Acceptance-Criteria.md` says "the nav's Invoice Queue sub-links show all 15 GB Skips statuses," which has never literally been true and appears to conflate the API's correct 15-status response with the nav's own, separate, always-14 design. Needs an Architect ruling: correct the acceptance-criteria wording, or change the nav to show all 15 including terminal ones — not resolved unilaterally by QA. | Next touch of the nav or the acceptance criteria doc | Low — documentation/ruling needed, not a functional defect | WP-025 QA report, 2026-08-03 (`docs/QA/WP-025-Sprint1-QA-Report.md`, Note LOW-1) |
| ~~**WP-078 path-filter: no genuine same-side-only positive test yet**~~ — closed by WP-085's real frontend-only push (2026-08-04), which turned out **not** low-risk: `paths-filter` itself resolved `frontend-changed=true`/`backend-changed=false` correctly, but `deploy-web`'s `needs: [detect-changes, migrate-development-database]` combined with a plain `if:` (no `always()`) meant the implicit `success()` AND across `needs` silently skipped `deploy-web` too, since `migrate-development-database` correctly skipped itself for the same push. CI showed fully green with nothing actually deployed. Fixed in `ci-cd.yml`'s `deploy-web` job: explicit `always()` + `needs.migrate-development-database.result != 'failure'/'cancelled'` check, plus an explicit `event_name` restriction (previously an accidental side effect of the same bug) so `pull_request` runs still can't deploy. `deploy-api` was never affected — its own gate (`backend-changed`) always matches `migrate-development-database`'s gate. | — | Closed — real bug found and fixed | WP-085 investigation, 2026-08-04 (superseded the 2026-08-03 "low-risk" Architect note, which was based on an untested assumption) |
| **`GetFolderCountsAsync` issues one `SearchAsync` call per non-terminal status (~11 requests per folder view)** — reuses the existing, already-tested `SearchAsync` (`PageSize: 1`, reading only `TotalCount`) rather than a new single `GROUP BY` repository query. A reasoned simple default, not a performance defect at current invoice volumes. | If invoice volume ever makes ~11 requests per folder view a real cost | Low, deferred | `docs/WP-059-Supplier-Folder-Cors-Doc-Decisions.md` |
| **Ruled: `formatDateTime` timezone (viewer-local vs fixed UTC)** — merging WP-020a reintroduced a change (present in the drop, undocumented by its own decision doc) that drops `timeZone: 'UTC'` from `format.ts`'s `Intl.DateTimeFormat` call. Invoice/note timestamps render in each viewer's local browser timezone instead of a consistent UTC value — a real, user-visible change for what is arguably an audit-relevant field (`01_Project_Context.md` §6: "Auditability built into the data model"). Kept as-is per explicit Product Owner instruction during the merge; tests were made deterministic via `vite.config.ts`'s `test.env: { TZ: 'UTC' }` rather than by re-forcing UTC in the source, so either ruling can be applied without further test changes. **Chief Technical Architect ruling (reaffirmed a second time, 2026-08-02, after reappearing once already from a stale merge): accepted as the Product Owner's call, viewer-local rendering stands. Recommend a timezone marker next to audit-relevant timestamps eventually — non-blocking, not a code change now.** | Closed — no further action unless new information arises | **Ruled — do not reopen without new information** | `docs/WP-020-Real-Auth-And-Api-Integration-Decisions.md` §8, raised during this session's merge, 2026-07-26 |
| **Ruled: npm audit 12 vulnerabilities (1 critical) from WP-020a's toolchain downgrade** — merging WP-020a as-is (per explicit Product Owner instruction — the drop was built from a stale `main` snapshot) reintroduced React 19→18, react-router-dom 7→6, Vite 8→5, Vitest 4→2, TypeScript ~6→^5.6, and ESLint 10→9. `npm audit` now reports 5 moderate/6 high/1 critical (the critical is a dev-only `@vitest/mocker`/Vite chain, CWE-862, CVSS 9.8 — not shipped to production, but a real dev-toolchain exposure). `npm audit fix --force` was not run since it would itself be an uncoordinated breaking-change decision. **Chief Technical Architect ruling (reaffirmed a second time, 2026-08-02): accept as-is, dev-only exposure, not shipped to production.** | Before this repo is opened to a wider team, or before any formal security review — whichever comes first | **Ruled — accepted, monitor trigger only** | `docs/WP-020-Real-Auth-And-Api-Integration-Decisions.md` §8, raised during this session's merge, 2026-07-26 |
| **Reconcile `FixtureWorkflowActionClient` (WP-018, dev-fallback only) against the real, now-deployed workflow-actions API** — `GET /api/invoices/{id}/available-actions` and `PATCH /api/invoices/{id}/status` are delivered and now the live path (`HttpWorkflowActionClient`, WP-020) — this fixture is retained only as a local-dev fallback (`workflowActionClient.ts`'s last line can be swapped back to it if the real API is unreachable), not a blocker for anything live. **Note (2026-07-26):** merging WP-020a as-is reverted this fixture's 2026-07-25 Architect-ruled correction (four gated transitions) back to a pre-ruling single-edge state, per explicit Product Owner instruction — flagged in `docs/WP-020-Real-Auth-And-Api-Integration-Decisions.md` §8. The fixture's remaining edges (the 3 GB-Skips-specific edges + the platform-default template's remaining ~23 transitions) still haven't been reconciled against WP-053's full 57-row confirmed graph — only relevant if/when this dev-fallback is next touched. | If/when the fixture fallback is next relied on, or WP-053's full table becomes available | **Low, deferred (dev-fallback only, not live)** | `docs/WP-018-Invoice-Workflow-Actions-Decisions.md` §1; `docs/WP-020-Real-Auth-And-Api-Integration-Decisions.md` §§5, 8 |
| **Per-Tenant Graph Configuration** — redesign `GraphOptions` from a single app-wide config to per-tenant data (`TenantGraphConfiguration` table in Azure SQL), multi-tenant Entra app registration with per-customer admin consent (client secret or certificate per tenant, certificate preferred), Key Vault secrets under a `graph-secret-{tenantId}` naming convention, and a per-tenant (or removed-from-shared-endpoint) health check. Owned by the Chief Technical Architect (design) / a Backend Engineer (implementation), as its own dedicated WP. | Before any second customer's mailbox is connected | **Blocking (future)** | `docs/WP-004-Graph-Multitenancy-Decision.md` |
| **Component tags on health check responses** — add a `"component"` field (`"graph"` / `"blob"` / `"database"`) to each entry in the `/health/ready` response body, so monitoring can filter/alert per component without a new endpoint. Small addition to `GraphMailboxHealthCheck`/`BlobStorageHealthCheck`. **Still open as of WP-024** (2026-08-02): that WP's alert rules work around the gap by distinguishing SQL/Blob/Graph failures via Application Insights' own automatic dependency telemetry (`type`/`target`) instead of this tag - a real, functioning workaround, but not a substitute for actually closing this item if a future consumer needs the tag itself (e.g. a UI status page reading `/health/ready` directly). | Next touch of WP-004 or WP-005 | Low, small | `docs/WP-004-Health-Check-Severity-Decision.md`; `infra/README.md` "What changed in wp-024" |
| **Deployment slot to eliminate the recurring post-deploy blip** — WP-024 root-caused the intermittent first-request-after-deploy 404/500 (WP-073/074/075) to the App Service plan's Basic B1 tier: single instance, no deployment slot, so every deploy fully recreates the container (confirmed via `AppServicePlatformLogs`/`AppServiceHTTPLogs` - invisible to Application Insights itself, which has zero `requests` rows for either failing request). Genuinely eliminating it needs a Standard-tier upgrade (cost increase from ~£10/month B1) plus a deployment slot with slot-swap wired into `.github/workflows/ci-cd.yml` - a real cost/architecture tradeoff, not implemented under WP-024's monitoring scope. | If zero-downtime deploys become a real requirement | **Needs scope decision** | `infra/README.md` "What changed in wp-024"; WP-024 report, 2026-08-02 |
| **`IInvoiceRepository.GetBySupplierAsync`** — targeted, indexed supplier-scoped read, to replace the full in-memory `GetAllAsync()` scans currently used by `DuplicateDetectionService.CheckAsync` and `InvoiceProcessingService.ResolveSupplierAsync`. | When invoice volume makes `GetAllAsync()` costly | Low, deferred | `docs/WP-010-Duplicate-Flag-Persistence-Decision.md` (also noted independently in `docs/WP-012-Invoice-Processing-Pipeline-Decisions.md` item 6) |
| **Supplier fuzzy-matching / alias resolution** — `ResolveSupplierAsync`'s case-insensitive trimmed exact-match will create duplicate `Supplier` rows for OCR/extraction variance (e.g. "Acme Ltd" vs "Acme Ltd." vs "ACME LIMITED"). No fuzzy-matching scheme, alias table, or manual-merge capability exists. Approved for MVP as-is; this is a flagged gap, not a blocker — no scheme should be invented speculatively. | When supplier-name variance from OCR becomes a real data-quality problem | Low, deferred | `docs/WP-012-Invoice-Processing-Pipeline-Decisions.md` item 3 (Ruling, 2026-07-25) |
| **Azure OpenAI resource still does not exist** — confirmed live: `az resource list -g rg-apflow-dev` shows Document Intelligence now provisioned (WP-061), but no Azure OpenAI resource. `infra/scripts/set-platform-ai-secrets.sh` remains a combined script requiring both Document Intelligence AND OpenAI key/endpoint values, so it still cannot be run as-is even though Document Intelligence alone is now resolved — WP-061 stored `docintel-secret` directly via `az keyvault secret set` instead. **Chief Technical Architect note (2026-08-02): nothing currently shipped uses Azure OpenAI for anything — the only OpenAI-dependent feature ever raised (AI-assisted supplier query drafting) is explicit Phase 2 scope per the GB Skips requirements addendum, not Sprint 1. Deprioritised below "needs scope decision" — do not provision speculatively ahead of an actual feature needing it.** | Before any actual OpenAI-dependent feature is scoped for implementation | **Low, deferred (not currently blocking anything shipped)** | `docs/WP-023-Report.md` "Flagged, not resolved" §1; WP-061 report, 2026-07-31; reaffirmed 2026-08-02 |
| **WP-026: `Supplier.PaymentTermsDays` uses a plain "net days" integer, not a named-terms catalogue** — `docs/AI/06_Domain_Reference_Data.md` has no payment-terms catalogue/convention (checked before implementing, per that document's own "escalate, don't invent" rule); chose the simplest representation that still round-trips through a Sage 50-style export (WP-042) rather than inventing named terms (`"Net 30"`/`"Due on Receipt"`/`"End of Month"`/etc.). Revisit if WP-042's real Sage 50 integration requirements turn out to need named/non-numeric terms this representation can't express. | If WP-042 (Sage 50 export) finds `net days` insufficient | Low, deferred (reasoned minimal default, not a gap) | `docs/WP-026-Supplier-Management-Report.md` |
| **WP-026: `Supplier.Status` uses a minimal `ACTIVE`/`INACTIVE` flag, not a richer state machine** — same reasoning as the payment-terms item above: `docs/AI/06_Domain_Reference_Data.md` has no supplier-status catalogue, so a new `SupplierStatusCodes` constants class (mirroring `InvoiceStatusCodes`'s own pattern) was added with just these two values, string-typed (not an enum) for consistency with `Invoice.Status`. No workflow/transition enforcement exists around this field (unlike `Invoice.Status`) - any caller can set either value via `PUT /api/suppliers/{id}`. Revisit if a future WP needs e.g. a "Suspended"/"Under Review" state or transition rules. | If a future WP needs more than a binary active/inactive flag | Low, deferred (reasoned minimal default, not a gap) | `docs/WP-026-Supplier-Management-Report.md` |
| **`APFlow-Spa-Dev` app registration's `isFallbackPublicClient` was flipped to `true`** — enabled during WP-064 to allow a device-code OAuth grant (used to acquire real test-user tokens for API-level role-gating verification, since no browser automation exists in this environment). **Still genuinely in active use** — ROPC/device-code token acquisition was also used in WP-065, WP-066, and WP-073's live verification, not just WP-064. Low real risk (device code still requires a genuine interactive sign-in with real credentials, it's not a bypass), but it's a standing app-registration setting change, not requested by any WP. **Chief Technical Architect guidance (2026-08-02): leave enabled for now — still doing real work. Revert as a conscious decision, not "whenever convenient."** | **Before GB Skips' real users are ever onboarded** — must not still be enabled by then | Low, awareness only — firm trigger set | WP-064, 2026-08-01; reaffirmed 2026-08-02 |
## Closed

| Item | Closed by | Date |
|---|---|---|
| ~~WP-026: `docs\Sprint2-Plan.md` (cited as an earlier round's full spec) does not exist in this repo~~ — an earlier session's WP-026 work proceeded from a self-contained inline task brief that cited a `docs\Sprint2-Plan.md` not yet checked in at the time (confirmed via a repo-wide search then). The file now exists and is the sprint's actual source of truth; this session completed WP-026 against its real §3 spec (adding the `Code`/`Email`/`Phone` fields and the Credit Limit permission split the earlier round hadn't built), so the missing-doc gap no longer applies. | This session, while merging the earlier WP-026 work against the now-current `docs/Sprint2-Plan.md`, 2026-08-07 | 2026-08-07 |
| ~~Application Insights' logging provider silently dropped every `LogInformation` call~~ — discovered while root-causing WP-082 (`traces | summarize count() by severityLevel` over 7 days: 754 Warning, 8 Error, 2 Verbose, **0 Information** — not sampling, a structural gap). `ApiServiceCollectionExtensions.cs` called `services.AddApplicationInsightsTelemetry()` (WP-066) without ever explicitly configuring `Logging:ApplicationInsights:LogLevel:Default`, so the AI logging provider's own separate, undocumented default (`Warning`) applied — distinct from and not overridden by the generic `Logging:LogLevel:Default: "Information"`, which only ever governed other providers (console, etc.). Resolved via WP-083: added an explicit `Logging:ApplicationInsights:LogLevel:Default: "Information"` section to `appsettings.json`/`appsettings.Production.json`/`appsettings.Development.json`. Volume/noise judgment call made alongside: surveyed every `LogInformation` call site first (~30 across the codebase) — per-cycle/per-operation summaries (pipeline run start/complete, email sync start/complete, `MarkAsProcessedAsync` success, CRUD operations, duplicate-detection results) stay at Information as genuinely operationally meaningful and not excessively frequent; `DocumentAnalysisService.LogConfidence` (the one call site invoked *per extracted field*, 8x per invoice — the highest-cardinality logger in the codebase) downgraded to `LogDebug`, since the per-invoice presence summary immediately above it already covers Information-level diagnostics without that multiplier. **Live-verified post-deploy**: before the fix, a real KQL query confirmed zero `severityLevel == 1` rows over 7 days; immediately after deploy, a real poll cycle produced 8 real Information-level traces on first try — "EmailIngestionWorker starting - polling every 60s", "Email ingestion poll cycle starting", "Invoice processing pipeline run starting", "Starting email sync for mailbox...", "Email sync completed...", "Synced 0 unprocessed email(s).", "Invoice processing pipeline run complete...", "Email ingestion poll cycle complete..." — exactly the pipeline-summary lines that had been invisible since WP-066. | WP-083 (Backend Engineer work package), 2026-08-03 | 2026-08-03 |
| ~~WP-025 MEDIUM-1: Header's "Signed in as: {role}" label never rendered, for any user~~ — root-caused via a real decoded token from a live session (WP-025): `Header.tsx`/`deriveActingUser.ts` read `account.idTokenClaims.roles`, but Entra only issues app-role claims in tokens for the resource (audience) they're assigned against — these roles are assigned against the API app's service principal (WP-064), not the SPA's, so the ID token (audience = SPA) never carried `roles` at all; the access token (audience = API) correctly carries them, and is what real authorization already uses. Resolved via WP-081: `deriveActingUser.ts` no longer derives `roles` at all (return type narrowed to exclude it, with a test proving the ID token's `roles` claim is now ignored even when present); new `decodeAccessTokenRoles.ts` decodes the access token instead, reusing the existing `getAccessToken()` httpClient.ts already calls (no second, separate token-acquisition path); `AuthContext.tsx` fetches it once per signed-in account and composes it into `ActingUser`. New tests: unit coverage for the decode utility (malformed/absent-claim handling) and a full `AuthProvider` + `Header` integration test asserting the correct label renders from a mocked access token while the mocked ID token carries no roles claim at all — a direct regression guard against the original bug recurring. **Live-verified post-deploy**: signed in fresh (real interactive CIAM sign-in, not cached) as both accounts — `apflow-test-reviewer` shows "Signed in as: Standard Reviewer", `apflow-test-approver` shows "Signed in as: Full Approver". | WP-081 (Frontend Engineer work package), 2026-08-03 | 2026-08-03 |
| ~~WP-025 HIGH-1: `EmailIngestionWorker` reprocessed one no-attachment email 719 times before self-stopping~~ — root-caused via a direct Graph API read against the real mailbox (app-only client-credentials token, the message's actual live category list) plus a precise commit-timeline cross-check, neither of the two hypotheses raised by WP-025 was exactly right: the message's `APFlow-Processed` category **was** present and durable (confirmed live), and the exclusion query **does** correctly honor it today (confirmed live, zero messages returned) — ruling out both "never marked" and "query doesn't work." The real cause: the entire 2026-08-01 23:39 UTC – 2026-08-02 12:00:59 UTC reprocessing window **predates** `7bb3e04` (the `isRead`→category-filter fix, deployed 2026-08-02 14:17 UTC) by over two hours. During that window the live query still filtered on `isRead eq false` — and since nothing in the pipeline has ever touched `isRead` (only categories), an email that stays unread keeps reappearing regardless of category state. `MarkAsProcessedAsync` was almost certainly working correctly the whole time; the query it fed just didn't care. The loop stopped at 12:00:59 UTC (before the fix even deployed), consistent with a human reading the email in Outlook/OWA around then. Zero data loss/corruption throughout. **Independent of this root cause** (itself already fixed, incidentally, by `7bb3e04` before this investigation began): added a hard cap (`MaxIngestionIssueOccurrences = 5`) so any *future* bug of the same shape — a conversation that keeps coming back as "unprocessed" for any reason — stops being re-extracted and re-recorded after 5 occurrences, logging one distinct `LogError` each time it's skipped, while still attempting `MarkAsProcessedAsync` every cycle so a fixable cause can still self-resolve. New test proves retry-to-cap-then-stop; full backend suite green (377 tests). Also surfaced, and separately resolved via WP-083 (see above): Application Insights silently drops all `LogInformation` telemetry — the reason this investigation had to lean on a live Graph read and a git-log timeline instead of just reading the obvious success/failure logs. | WP-082 (Backend Engineer work package), 2026-08-03 | 2026-08-03 |
| ~~WP-080's live verification (resending Ahmed's exact two-email scenario against the deployed environment) was still outstanding~~ — the code fix, test coverage, and deploy are all done and confirmed (376 backend tests passing, `f97a9c0`/`395fa2d`/`015ebfa` live). The actual resend needs Ahmed specifically (it's his real scenario) and he was only around earlier in the day when this was found. **Product Owner instruction (2026-08-02): close this for now rather than leave it open indefinitely waiting on a specific person's availability — re-raise if the same silent-skip behavior is seen again.** Not a correctness gap in what shipped, purely a live-confirmation step deferred by choice. | Product Owner instruction, 2026-08-02 | 2026-08-02 |
| ~~Every newly-ingested invoice landed at `EXTRACTED`, requiring a pointless manual first PATCH to `AWAITING_REVIEW` before a reviewer could do anything else~~ — surfaced live during WP-064's verification (`G169833428` had to be manually advanced before "Mark Checked & Ready to Approve" ever appeared). Resolved via WP-071: `InvoiceProcessingService`'s candidate `Invoice` is now constructed directly at `InvoiceStatusCodes.AwaitingReview`, still within the existing single `AddAsync`/`SaveChangesAsync` commit - no second save. Confirmed this doesn't touch role-gating: this pipeline builds the `Invoice` directly via `IInvoiceRepository`, never calling `InvoiceService.UpdateAsync`, so `IWorkflowValidationService`/`IApprovalAuthorizationService` are bypassed by the existing architecture, not new code; `EXTRACTED → AWAITING_REVIEW` also isn't one of the four `RoleGatedTransitions` pairs regardless. Live-verified with a real test email: both resulting `Invoice` rows persisted with `Status = AWAITING_REVIEW` directly (log line and direct SQL query both confirm), and the real `InvoiceWorkflowActionsService.GetAvailableActionsAsync` code, run against the live database, shows `CHECKED_READY_TO_APPROVE` immediately available - no manual step. | WP-071 (Backend Engineer work package), 2026-08-01 | 2026-08-01 |
| ~~No CIAM test/demo users existed; unconfirmed whether `FINANCE_MANAGER`/`AP_REVIEWER` app roles were even defined on the API app registration~~ — confirmed live they were NOT (`appRoles: []` on `APFlow-Api-Dev`). Resolved via WP-064: both app roles added to the manifest; four users created in the `rameezjav` CIAM tenant (`apflow-{test,demo}-{approver,reviewer}@rameezjav.onmicrosoft.com`) with the correct role assigned to each via `appRoleAssignments` against the API's service principal; credentials stored in `kv-apflow-dev-ryd3y6` (`apflow-{account}-password` secrets), never in chat. Test pair live-verified end-to-end using real tokens (device-code OAuth grant, not ROPC) against the real API - not simulated: reviewer sees both WP-069 invoices, correctly sees `CHECKED_READY_TO_APPROVE` as an available action but never `APPROVED`/`NEEDS_QUERY` in its own available-actions list; a direct `PATCH .../status` attempt at `CHECKED_READY_TO_APPROVE→APPROVED` as reviewer returns `403 Approval.Unauthorized`, invoice confirmed unchanged; approver executes it successfully (`200`). All four `RoleGatedTransitions` pairs confirmed live (reviewer 403/approver 200 on `CheckedReadyToApprove→Approved`, `Rejected→AwaitingReview`, `Cancelled→Received`; the fourth, `CheckedReadyToApprove→NeedsQuery`, shares the identical authorization check already proven by the others). Walked one WP-069 invoice (`G169833428`) through the full real GB Skips flow (`EXTRACTED→AWAITING_REVIEW→CHECKED_READY_TO_APPROVE→APPROVED`) as a rehearsal; used the second WP-069 invoice (`INV-100`) purely for the reopen-path tests, left at `RECEIVED`. Demo pair confirmed provisioned identically (same role assignments) and left completely untouched - no sign-in, no API calls - per explicit instruction not to spend that narrative moment before the real demo. | WP-064 (DevOps + Backend Engineer work package), 2026-08-01 | 2026-08-01 |
| ~~`WorkflowSeedData.GbSkipsPlaceholderTenantId` (`00000000-0000-0000-0000-0000000B5121`) was stale — GB Skips' real tenant id had been known and live since WP-021~~ — resolved via WP-070: the constant is renamed `GbSkipsTenantId` and its value corrected to `641fc267-7902-48d0-8e1c-1d3d0166c8ac` (the CIAM sign-in tenant every real GB Skips user authenticates through). A new migration (`20260731224417_CorrectGbSkipsTenantId`) re-keys every row that referenced the old placeholder - not just `WorkflowTemplate`/`StatusReference`/`WorkflowTransition` as originally scoped, but also `ApprovalPolicy`'s GB Skips row (same shared constant, found while tracing every reference before renaming it - leaving it on the stale value would have created a worse, inconsistent split-brain state). Pure `UpdateData` calls keyed by `Id`, no data loss, fully reversible `Down()`. Live-verified two ways: (1) direct SQL query confirms 15 `StatusReferences`, 32 `WorkflowTransitions`, and the `ApprovalPolicy` row all now carry the correct tenant id; (2) the real `WorkflowTemplateRepository.GetActiveTemplateAsync` code (not a re-derivation) run against the live database resolves `"GB Skips Invoice Workflow"` (15 statuses) for a caller with `tid = 641fc267-...`, vs. `"Platform Default"` (13 statuses) for any other tenant - exactly the counts WP-070 asked to confirm. Data-only fix - no API code redeploy needed, since `GetActiveTemplateAsync` resolves purely from live table data, never from the C# constant at runtime. Confirmed the two WP-069 invoices are untouched (`ModifiedAtUtc` still blank, `TenantId` still correct) - only the template lookup was ever wrong, not invoice data. | WP-070 (Backend Engineer work package), 2026-07-31 | 2026-07-31 |
| ~~WP-063 — Prove End-to-End Ingestion Against a Real Mailbox~~ — closed, not paused: fully satisfied as a side effect of WP-069's own live verification, with direct evidence for every one of WP-063's original deliverables. Real email in (`EmailSyncService`: "Email sync completed... 1 unread message(s) found"/"2 unread message(s) found"), real Graph read (200, not 401 — WP-068), real PDF extraction (`PdfExtractionService` log output), real Blob upload (both PDFs confirmed present in `stapflowdevryd3y6fyfloxu`/`documents`, correctly tenant-prefixed), real SQL row (two new `Invoice` rows confirmed via a direct AAD-authenticated SQL query, correct `TenantId`). The two test emails turned out to be genuinely distinct invoices rather than a duplicate pair, so WP-063's original duplicate-flagging check wasn't exercised by these two specifically — everything else in its brief was proven with direct evidence (log output, SQL query, Blob listing), not just described. `/health/ready`'s Graph status is `Healthy` (WP-068), not `Degraded` — the mailbox connection is both nominally and actually working now, so WP-004's "Degraded may be expected regardless of actual connectivity" question is moot for this environment. | WP-069's live verification (Backend Engineer work package), 2026-07-31 | 2026-07-31 |
| ~~Background worker (`EmailIngestionWorker`) could not persist anything: `BlobStorageService.ScopeBlobName` failed every attachment upload 3/3 times with `BlobStorage.NoTenantContext`~~ — confirmed live right after Graph auth was fixed (WP-068) and two real test emails finally reached the pipeline: `CurrentUserService.TenantId` reads the `tid` claim off `IHttpContextAccessor.HttpContext?.User`, and the worker's per-cycle DI scopes have no HTTP request at all, so it was always null. The same null tenant would also have caused `AppDbContext`'s tenant query filter/audit stamping to write new rows with `TenantId = Guid.Empty` (orphaned, invisible to any real user) had a save ever been reached. Ruling: don't derive tenant context from the Graph mailbox's own tenant (a different, mail-only M365 tenant — mixing the two would be a real architectural error) or add a second, independent config value — reuse GB Skips' actual AP Flow tenant id. That id turned out NOT to be `WorkflowSeedData.GbSkipsPlaceholderTenantId` (still the placeholder, confirmed unfixed — see the new open item above) but the real CIAM sign-in tenant (`641fc267-7902-48d0-8e1c-1d3d0166c8ac`, live since WP-021, already used throughout `infra/main.dev.bicepparam`/`resources.bicep`) — the value a real signed-in GB Skips user's own `tid` claim actually carries. Resolved via WP-069: a new `WorkerCurrentUserService` (`src/APFlow.Infrastructure/Security/WorkerCurrentUserService.cs`) returns this tenant id directly; `DependencyInjection.AddInfrastructure`'s `ICurrentUserService` registration now routes to it only when the resolving scope has no live `HttpContext` (the real, HTTP-path `CurrentUserService` class is completely unmodified and still resolves identically for every actual request). New `Workers__TenantId` app setting in `resources.bicep`, reusing the existing `entraTenantId` bicep parameter rather than a second value. Live-verified after redeploy: the two previously-stuck unread emails were both processed - two new `Invoice` rows created (`SupplierInvoiceNumber` `INV-100` and `G169833428`, confirmed via a direct SQL query with `TenantId = 641fc267-...` on both), both PDFs confirmed present in Blob Storage under the correct tenant-scoped path (`641fc267-.../invoices/...`). Neither idempotency nor duplicate-detection triggered on these two, because - contrary to the assumption going into this WP - they turned out to be two genuinely distinct invoices (different supplier, different invoice number, different content hash, different amount), not the same content under different filenames. | WP-069 (Backend Engineer work package), 2026-07-31 | 2026-07-31 |
| ~~Graph mailbox sync genuinely failed live with a `401 Unauthorized` from Microsoft Graph itself~~ — confirmed root cause via live `az webapp config appsettings list`: neither `AzureKeyVault__Enabled` nor `AzureKeyVault__VaultUri` had ever existed as app settings (`KeyVaultOptions.Enabled` defaults to `false`), so `Program.cs`'s `AddAzureKeyVault` call had never executed all session. Independently confirmed the credential-selection logic itself (`AddGraph` in `APFlow.Integrations`) was already correct — it prefers `ClientSecretCredential` whenever `ClientSecret` is non-blank; the bug was purely that `ClientSecret` was always blank, forcing the Managed-Identity fallback. Fixing the generic Key Vault toggle alone would still not have been enough: the Graph secret is deliberately stored as `graph-secret-{tenantId}` (`docs/Secret-Naming-Convention.md`), which the generic `AddAzureKeyVault` name-mapping does not match. Resolved via WP-068 (`576f2b6`): added `AzureKeyVault__Enabled`/`VaultUri` app settings in `resources.bicep`, and an explicit `SecretClient.GetSecret($"graph-secret-{tenantId}")` read in `Program.cs` injected into `Graph:ClientSecret`. Redeployed live (infra via `az deployment sub create`, code via zip-deploy) and confirmed: `/health/ready` now returns `Healthy` (was `Degraded`), and three consecutive live `EmailIngestionWorker` poll cycles completed with `0 failed` each — a real `200` from Graph, not a `401`. Unblocks WP-063. | WP-068 (Backend Engineer work package), 2026-07-31 | 2026-07-31 |
| ~~`IInvoiceProcessingService.ProcessUnreadEmailsAsync` (the full, correct WP-012 ingestion pipeline) was never called by anything outside tests — `APFlow.Workers`'s `AddWorkers()` was an empty stub, no scheduled/background poller existed anywhere~~ — discovered while investigating WP-063 ("send a real email, prove it becomes an invoice") and confirmed by grepping the whole repo for every call site. Resolved via WP-067: `EmailIngestionWorker` (`BackgroundService`, timer-based, `Workers:EmailPollingIntervalSeconds` config, default 60s) now calls it on a loop, registered as a real hosted service. Live-verified: deployed, healthy on first check, logged two consecutive poll cycles exactly 60s apart, survived a cycle failure without crashing the host (see the new Graph 401 item above, discovered *by* this same live verification). | WP-067 (Backend Engineer work package), 2026-07-31 | 2026-07-31 |
| ~~`SQL_SERVER_FQDN`/`SQL_DATABASE_NAME`/`API_APP_SERVICE_NAME`/`WEB_APP_SERVICE_NAME` GitHub Environment variables unconfirmed; no fully-green pipeline run had ever happened~~ — resolved via WP-062: user re-confirmed all four on `development`, a genuinely fresh push-triggered run (`2be87d2`, run [30658668711](https://github.com/AlamgeerRao/APFlow/actions/runs/30658668711)) completed with **every one of its 10 jobs succeeding** — all 5 backend test-matrix legs, frontend build/lint/type-check/test, `Publish APFlow.Api`, `Apply EF Core migrations (Development)`, `Deploy APFlow.Api (Development)`, `Deploy APFlow.Web (Development)`. Also fixed a flaky `httpClient.getBlob` test along the way (CI-only failure, never reproduced locally): wrapping a real `Blob` in a real `Response` and round-tripping it through `.blob()` is inconsistent across jsdom versions — replaced with a minimal `{ ok, status, blob() }` object literal covering exactly what `getBlob` touches. First time this pipeline has ever gone fully green end to end. | WP-062 (DevOps Engineer work package), 2026-07-31 | 2026-07-31 |
| ~~`app-apflow-dev-api` crash-loops without a real Document Intelligence resource; non-durable `ASPNETCORE_ENVIRONMENT=Development` live workaround in place~~ — resolved via WP-061: provisioned a real `Microsoft.CognitiveServices/accounts` (kind `FormRecognizer`, S0/Standard) in `resources.bicep`. First deploy attempt failed (`ukwest` doesn't support this account type at all, confirmed via `az cognitiveservices account list-skus`) — fixed by hardcoding this one resource's location to `uksouth` (still UK data residency, just the specific region that supports it), everything else stays in `ukwest`. Only the non-secret endpoint is wired into `DocumentIntelligence__Endpoint`; the key is stored in Key Vault (`docintel-secret`) for reference/break-glass only, never wired into the app — auth goes through `DefaultAzureCredential` backed by a `Cognitive Services User` RBAC grant (confirmed live via `az rest` against the role-assignments API), matching the existing Graph/BlobStorage/SQL secretless pattern. `ASPNETCORE_ENVIRONMENT=Development` removed entirely (not reapplied after this redeploy, unlike every prior redeploy this session). Live-verified: clean restart with no new crash (the one `exit code 134` visible in logs is Azure's stale carried-forward error field from an unrelated incident hours earlier, confirmed by its frozen timestamp), `/health/ready` → `200`/`Degraded` (expected), `/openapi/v1.json` → `401` with an empty body and `WWW-Authenticate: Bearer` (the actual document is no longer returned to an unauthenticated caller — was `200` with the full document before this fix). | WP-061 (DevOps Engineer work package), 2026-07-31 | 2026-07-31 |
| ~~`main.bicep`'s blank `entraTenantId`/`entraSpaClientId`/`entraApiClientId`/`entraApiScope` defaults were a silent-data-loss hazard on redeploy~~ — flagged by the DevOps engineer reviewing the 2026-07-31 redeploy: a code comment noting "pull the live values first" isn't structural insurance against the next redeploy (manual or CI-triggered) omitting them and quietly blanking out working sign-in config. Considered removing the defaults entirely (fail loudly at validation) but that would break the intentional bootstrap sequence for a genuinely new environment (`main.bicep`'s own comment: "Safe to leave blank on first deploy; re-run the deployment after populating them"). Resolved instead with a checked-in `infra/main.dev.bicepparam` params file (no secrets — just IDs/a UPN login) holding this environment's real values as a canonical source, validated live via `az deployment sub validate --parameters infra/main.dev.bicepparam` (`provisioningState: Succeeded`). `infra/README.md`'s deployment-order steps 1 and 3 now point at this file as the primary path; the manual `--parameters` form is kept only for bringing up a genuinely new environment that has no params file yet. | This session, in response to DevOps's review comment | 2026-07-31 |
| ~~`APFlow.Web` had no static-file server (`server.js`) — WP-022's `deploy-web` job would ship a non-functional deployment~~ — resolved: added `src/APFlow.Web/server.js` (Express, `express.static('dist')` + a catch-all fallback to `index.html` for client-side routing), added `express` as a runtime dependency, and added a `"start": "node server.js"` script. Verified locally end-to-end after `npm run build` — static assets, JS bundle, and a synthetic SPA route (`/some/spa/route`) all returned 200 with the right content. Surfaced by the DevOps engineer's live diagnosis (`az webapp log tail` showing Azure's generic static-site fallback running instead) while investigating why `app-apflow-dev-web-ryd3y6fyfloxu` only ever showed a placeholder. | This session, in response to DevOps's live `deploy-web` diagnosis | 2026-07-31 |
| ~~Neither `APFlow.Api` nor `APFlow.Web` had ever received a real deployment~~ — resolved across four chained fixes, all 2026-07-31, ending in confirmed real sign-in: (1) `SCM_DO_BUILD_DURING_DEPLOYMENT: true` made Oryx auto-run `package.json`'s `"build"` script against an artifact shipping only pre-built `dist/`, not `tsconfig.json`/`src/` (`error TS5083`) — fixed by shipping a production-only `node_modules` (`npm ci --omit=dev`) and setting `SCM_DO_BUILD_DURING_DEPLOYMENT: false`, resolving the DevOps-flagged Oryx-vs-ship-`node_modules` question in favor of the latter. (2) `deploy-web` then succeeded but the app failed in-browser (`MSAL initialization failed: Missing required environment variable: VITE_ENTRA_CLIENT_ID`) — `VITE_*` vars are compiled into the bundle at `vite build` time, not read at runtime; wired `VITE_ENTRA_CLIENT_ID`/`VITE_ENTRA_AUTHORITY`/`VITE_API_SCOPE`/`VITE_API_BASE_URL` into `ci-cd.yml`'s `Build production bundle` step, including a **brand-new** `ENTRA_AUTHORITY` GitHub variable never documented before (`https://rameezjav.ciamlogin.com/641fc267-7902-48d0-8e1c-1d3d0166c8ac`, live-verified via its own `/.well-known/openid-configuration`). (3) The very next run failed that fix's own fail-loud check — `frontend-build-test` never declared `environment: name: development`, so every `vars.*` in that job silently resolved empty even though the variables were genuinely set; fixed by adding the missing `environment:` block, the only job with `vars.*` reads that lacked it. (4) User confirmed all 11 required GitHub variables present. **DevOps then confirmed live in-browser: sign-in works end-to-end.** | This session, ending in DevOps's live sign-in confirmation | 2026-07-31 |
| ~~`Cors:AllowedOrigins` was never set live on `app-apflow-dev-api`, blocking the Invoice Queue from loading any data~~ — DevOps confirmed via DevTools: a clean CORS preflight failure (`No 'Access-Control-Allow-Origin' header is present`) once sign-in was working and the Invoice Queue tried to call the API. Code (`Program.cs`'s middleware ordering, `ApiServiceCollectionExtensions.cs`'s policy/fail-closed design) was already correct — `az webapp config appsettings list` confirmed `Cors` returned `[]` live. Added `Cors__AllowedOrigins__0` to `resources.bicep`'s `apiAppService`, built from the `webAppServiceName` string variable (not `webAppService.properties.defaultHostName`, which would create a circular resource dependency). Redeployed and confirmed live via a real `OPTIONS` preflight — `Access-Control-Allow-Origin` correctly echoed. | This session, in response to DevOps's live CORS diagnosis | 2026-07-31 |
| ~~Fixing the CORS setting above forced `app-apflow-dev-api`'s first real restart in a while, which crash-looped (exit code 134/SIGABRT, tens of seconds per attempt, zero application-level stdout ever captured)~~ — root-caused by comparing a live `ASPNETCORE_ENVIRONMENT=Development` restart (survived) against the default/unset value (Production, crash-loops): `AuthenticationExtensions.cs`, `Infrastructure`/`Integrations`'s `DependencyInjection.cs` all share `if (!isConfigured && !environment.IsDevelopment()) throw ...` for `EntraId`, `ConnectionStrings:DefaultConnection`, `BlobStorage`, and `Graph` — every one of these was a live-app-setting-name mismatch identical in kind to `RESOURCE_GROUP`/`SQL_SERVER_FQDN`/the `VITE_*` vars (e.g. `ENTRA_TENANT_ID`/`SQL_SERVER_FQDN`/`STORAGE_ACCOUNT_NAME` are flat names read by *no* .NET code at all — confirmed by grepping `src/` for their literal names, zero matches). Fixed by adding `EntraId__Authority`/`EntraId__Audience` (with a new `entraAuthority` bicep parameter, live-verified via the authority's own `/v2.0/.well-known/openid-configuration`, and the API app registration's actual Application ID URI via `az ad app show`, not just its bare client ID), `ConnectionStrings__DefaultConnection` (AAD-authenticated, matching `ci-cd.yml`'s own migration connection-string format), `BlobStorage__ContainerName`/`AccountUrl`, and `Graph__TenantId`/`ClientId`/`MailboxUserPrincipalName` (three new bicep parameters, values already established as this environment's `GRAPH_*` GitHub variables) — `Graph:ClientSecret` deliberately left unset, falling back to this app's own Managed Identity by design. **`DocumentIntelligence:Endpoint` hits the identical fail-fast check but has no real resource to point at — see the open item above, not resolved here.** Confirmed live after redeploy: `/health/ready` returns `Degraded` (Graph/Blob checks are designed to report this without failing readiness, per `WP-004-Health-Check-Severity-Decision.md` — Database is healthy, since an actual failure there would report `Unhealthy`, not `Degraded`), a real CORS preflight passes, and `GET /api/invoices` returns a clean `401` (reachable, auth enforced) rather than a crash or CORS block. | This session, following the CORS fix above | 2026-07-31 |
| ~~WP-020 — Real Microsoft Entra sign-in in `APFlow.Web`, amended scope~~ — real MSAL/Entra sign-in replacing WP-014's demo-tenant/role picker stub; a central `fetch`-based API client (`httpClient.ts`) with Bearer auth, session-expiry re-sign-in, and GET-only retry; and WP-015/016/017/018's fixture clients swapped for real HTTP ones (WP-019 stays fixture-backed — new item above). Live verification against a real Entra tenant/backend could not be performed (flagged, not assumed) — see `docs/WP-020-Manual-Verification-Checklist.md`. Merging this WP's zip (a stale `main` snapshot) also reintroduced a toolchain downgrade and reverted two prior fixes — see the three items above and `docs/WP-020-Real-Auth-And-Api-Integration-Decisions.md` §8. | This session's merge - see `docs/WP-020-Real-Auth-And-Api-Integration-Decisions.md` | 2026-07-26 |
| ~~The CI/CD one-time setup (`setup-github-oidc-service-principal.sh`) had likely never actually been run~~ — resolved: the user ran it for real, with `--sql-server-name sql-apflow-dev-ryd3y6fyfloxu` (the post-wp-060 argument). `APFlow-CI-Dev` app registration created in tenant `641fc267-7902-48d0-8e1c-1d3d0166c8ac`, Client ID `c49371f6-7723-42a7-b9e0-ae0e39b4bf1b`, no client secret. `Website Contributor` granted on `rg-apflow-dev`; `SQL Server Contributor` granted on `sql-apflow-dev-ryd3y6fyfloxu`. `grant-ci-sql-migration-access.sql` run via the Portal Query Editor (Microsoft Entra Authentication — `sqlcmd` wasn't available in the Cloud Shell image used) — confirmed `APFlow-CI-Dev` / `EXTERNAL_USER` / `EXTERNAL` present in `sys.database_principals` with `db_ddladmin`/`db_datareader`/`db_datawriter`. `CI_AZURE_CLIENT_ID`, `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID` added as GitHub Environment variables under `development`. | User completed the one-time setup directly, 2026-07-31 | 2026-07-31 |
| ~~CI/CD OIDC federated credential had the wrong subject shape — `AADSTS700213`~~ — the first real pipeline run after the above failed Azure login: `migrate-development-database`/`deploy-api`/`deploy-web` all specify `environment: name: development`, which gives GitHub's OIDC token an environment-scoped subject (`repo:{org}/{repo}:environment:{name}`) that takes priority over the ref-scoped one the setup script originally created — not in addition to it; this account also includes numeric owner/repo IDs in the subject. Resolved: the user added the two missing federated credentials directly to the existing `APFlow-CI-Dev` app (object id `744112fe-2263-4194-adb3-bba7af331a1b`) — `github-actions-env-development` (`repo:AlamgeerRao/APFlow:environment:development`) and `github-actions-env-development-idscoped` (`repo:AlamgeerRao@105811261/APFlow@1302101224:environment:development`), both confirmed created. `setup-github-oidc-service-principal.sh` updated to create all 3 shapes for future setups. | User added the federated credentials directly, 2026-07-31 | 2026-07-31 |
| ~~GitHub-hosted runners can't reach Azure SQL without a firewall rule for their own ephemeral IP~~ — the first real `dotnet ef database update` run failed with `SqlException` error 40 (`Could not open a connection to SQL Server`, TCP Provider). Azure SQL's "Allow Azure services" firewall rule (`0.0.0.0`–`0.0.0.0`) only covers Azure's own trusted PaaS-to-PaaS traffic, not arbitrary internet hosts like GitHub-hosted `ubuntu-latest` runners. Fixed in `ci-cd.yml`: "Add/Remove temporary SQL firewall rule" steps around the migration, scoped to exactly that run's IP via `curl https://api.ipify.org`, removed unconditionally afterward (`if: always()`). Required the CI service principal to hold a role covering `Microsoft.Sql/servers/firewallRules/*` on the SQL server resource (`SQL Server Contributor`, granted) and a `RESOURCE_GROUP` GitHub Environment variable (`rg-apflow-dev`) for the steps' `--resource-group` flag — both confirmed set 2026-07-31. **All prerequisites for this fix are now in place; still not exercised by an actual real pipeline run.** | User confirmed `RESOURCE_GROUP=rg-apflow-dev` set as a GitHub Environment variable, 2026-07-31 | 2026-07-31 |
| ~~WP-021 — dev Entra External ID (CIAM) tenant doesn't exist yet~~ — resolved: the CIAM tenant was created (`rameezjav.onmicrosoft.com`, tenant ID `641fc267-7902-48d0-8e1c-1d3d0166c8ac`, confirmed type External). | WP-021a merge — see `docs/WP-021-Azure-Infrastructure-Decisions.md` | 2026-07-29 |
| ~~WP-021 — `Mail.ReadWrite` has no mailbox to target if left in the CIAM tenant~~ — resolved by Chief Technical Architect ruling: the Graph/mail app registration lives in a separate M365 tenant with a real mailbox (Business Basic, monthly billing — the free Developer Program sandbox was tried first and found ineligible under a 2024 Microsoft policy change). Also corrected the API/Graph registration count from two to three (separate security boundaries). See `infra/docs/M365-Dev-Mailbox-Tenant.md`. | WP-021a merge — see `docs/WP-021-Azure-Infrastructure-Decisions.md` | 2026-07-29 |
| ~~Reconcile `FixtureInvoiceDetailClient` (WP-016) / `FixtureInvoiceClient` (WP-015) / `FixtureInvoiceNoteClient` (WP-017) / `FixtureWorkflowActionClient` (WP-018, live path) against their real backend shapes~~ — all four swapped to real HTTP clients as part of WP-020 (see above); three had field-name mismatches caught by QA and fixed (decision doc §7). | This session's merge - see `docs/WP-020-Real-Auth-And-Api-Integration-Decisions.md` | 2026-07-26 |
| ~~`Invoice.IsPotentialDuplicate` / `Invoice.DuplicateCheckReason`~~ — persisted fields on `Invoice`, written by `InvoiceProcessingService.PersistDuplicateCheckResultAsync` via a new `IInvoiceRepository` dependency; `DuplicateDetectionService` unchanged (still pure compute). | This session's implementation - see `docs/WP-010-Duplicate-Flag-Persistence-Decision.md`'s "## Implementation" section | 2026-07-20 |
| ~~Invoice Notes API endpoint~~ — `GET`/`POST /api/invoices/{id}/notes` added to `InvoicesController`, exactly as ruled: separate resource (not folded into `InvoiceDetailResponse`); `InvoiceNote.AuthorDisplayName` (new column, migration `AddInvoiceNoteAuthorDisplayName`) resolved server-side from `ICurrentUserService.DisplayName`. **Superseded/reworked the same day by WP-055** (Chief Technical Architect ruling): `AuthorDisplayName` is now `nvarchar(200)` (not 256, the original implementation's column length) and `ICurrentUserService.DisplayName` is now a required interface member (not default-implemented as null — all ~10 test-double implementers were updated). `AddNoteAsync`/`GetNotesAsync` still return `InvoiceNoteDto` (id/content/author/timestamp). WP-017's `FixtureInvoiceNoteClient` swap itself remains open, blocked on the WP-002 row above. | This session's implementation, superseded/reworked by WP-055 - see `docs/WP-017-Invoice-Notes-Decisions.md` §1 (Ruling, 2026-07-25) and `docs/WP-055-Invoice-Notes-Api-Decisions.md` | 2026-07-25 |
| ~~WP-056 — Persist per-field extraction confidence~~ — new `InvoiceExtractedField` child entity (migration `AddInvoiceExtractedFields`), populated by `InvoiceProcessingService` at ingestion, replaces `GET /api/invoices/{id}`'s `ExtractionConfidenceNote` placeholder with real per-field `extractedFields` data. Chief Technical Architect ruling (2026-07-25) on the WP's own flagged scope question: `Currency` is included as its own row (null `ConfidenceScore`), not excluded. | This session's implementation - see `docs/WP-056-Persist-Extraction-Confidence-Decisions.md` | 2026-07-25 |
| ~~WP-057 — Delete the `DUPLICATE_SUSPECTED` `StatusReference` row~~ — both templates' rows removed via migration `RemoveDuplicateSuspectedStatus` (two `DeleteData` calls, verified against a real database). `InvoiceStatusCodes.DuplicateSuspected` (the C# constant) deliberately kept for any historical `Invoice.Status` value. `docs/AI/06_Domain_Reference_Data.md` §2's stale `DUPLICATE_SUSPECTED` row (flagged, not edited unilaterally) is **now closed too** — the Architect's own maintained copy already had it removed; the mismatch was this repo's tracked copy lagging behind (same stale-copy problem as WP-053). Corrected 2026-07-25 by committing the Architect's copy directly. | This session's implementation - see `docs/WP-057-Retire-Duplicate-Suspected-Status-Decisions.md` | 2026-07-25 |
| ~~WP-058 — Restore `GET /api/invoices`~~ — re-added to `InvoicesController`, wiring the existing, already-tested `IInvoiceQueryService` (WP-011) — controller-only, no service-layer changes. Framing correction recorded in the WP's own report: this is a **new** endpoint, not a restoration (no list endpoint had ever previously shipped in this controller); the technical work is identical either way. Also fixes the stale WP-050-era comment on GB Skips' two tenant-specific statuses in `InvoiceStatusCodes.cs`. Swapping `FixtureInvoiceClient` (WP-015) for the real HTTP client remains WP-020's job, tracked in that row above. | This session's merge - see `docs/WP-058-Invoice-List-Endpoint-Decisions.md` | 2026-07-26 |
| ~~WP-019 — real backend endpoints for Supplier & Folder Views~~ — `GET /api/invoices/folders`, `/suppliers`, `/grouped` added to `InvoicesController` via new `ISupplierFolderQueryService`, composing the existing, already-tested `IWorkflowQueryService`/`IInvoiceQueryService` (no new repository method). `SupplierGroupDto.Invoices` reuses `InvoiceListItemDto` as-is (backend field names win over the frontend fixture's renamed ones, same reconciliation rule as WP-015/016). A real correctness fix was made before writing tests: both new read paths loop internally across `IInvoiceQueryService.SearchAsync` pages rather than capping at one page, since capping could silently drop a supplier's entire group past the 100-row page cap. Swapping WP-019's frontend fixture client for this real one remains a separate, not-yet-scheduled task. | This session's merge - see `docs/WP-059-Supplier-Folder-Cors-Doc-Decisions.md` | 2026-07-30 |
| ~~`APFlow.Api` had no CORS policy~~ — named policy (`ApFlowWebClient`) added, origins read from `Cors:AllowedOrigins` configuration (never hardcoded). Explicit config wins in every environment; a permissive `AllowAnyOrigin` fallback applies only in Development with no configured origins; any other environment with no configured origins fails closed (no cross-origin requests allowed at all). No `AllowCredentials()` (Bearer-token auth, not cookies). Decision logic extracted to a public `ConfigureCorsPolicy` method, unit-tested directly against a real `CorsPolicyBuilder`. | This session's merge - see `docs/WP-059-Supplier-Folder-Cors-Doc-Decisions.md` | 2026-07-30 |
