# Backlog

Items raised by architectural decisions but not implemented at the time the
decision was made. Each entry links back to the decision doc that created it.
Remove an entry once its work package/task lands and note the commit/WP that
closed it.

| Item | Trigger | Priority | Raised by |
|---|---|---|---|
| **WP-002 — Real Microsoft Entra sign-in in `APFlow.Web`** — `AuthContext.tsx` is still WP-014's demo-tenant/role picker stub; `package.json` has no auth library (no MSAL) and no HTTP client at all. The API requires a real, validated JWT Bearer token unconditionally, even in Development (no dev-auth bypass exists in `AuthenticationExtensions.cs`). Discovered while auditing the repo against Chief Technical Architect rulings (2026-07-25): every "swap fixture for real HTTP client" item below (WP-015/016/017/018) is blocked on this, not just on its own backend contract - a real HTTP client built today would 401 on every call. Owned by a Frontend/Backend Engineer pair, as its own dedicated WP. | Before any fixture→HTTP client swap (WP-015/016/017/018) can be attempted for real | **Blocking (near-term)** | Discovered 2026-07-25 auditing README/Backlog against recorded rulings |
| **Reconcile `FixtureWorkflowActionClient` (WP-018) against the real, now-deployed workflow-actions API** — `GET /api/invoices/{id}/available-actions` and `PATCH /api/invoices/{id}/status` are delivered (`docs/WP-054-Invoice-Workflow-Transition-Api-Decisions.md`), matching WP-018a's own confirmed contract shape (`{ targetStatusCode, targetStatusLabel }[]`) exactly. The two specifics this backlog item originally flagged are now handled: (1) use WP-054's full `InvoiceDetail` response directly, not a second `retry()`; (2) the fixture's role gating was corrected 2026-07-25 to cover all four real gated transitions (`CHECKED_READY_TO_APPROVE→APPROVED`, `CHECKED_READY_TO_APPROVE→NEEDS_QUERY`, `REJECTED→AWAITING_REVIEW`, `CANCELLED→RECEIVED`), not just one. **Still open:** the fixture's other edges (the 3 GB-Skips-specific edges + the platform-default template's remaining ~23 transitions) have not been reconciled against WP-053's full 57-row confirmed graph - left for the real HTTP client swap itself rather than a piecemeal fixture rewrite. Blocked on the WP-002 row above regardless. | Before WP-018's fixture client is replaced with a real HTTP-backed one | **Blocking (near-term)** | `docs/WP-018-Invoice-Workflow-Actions-Decisions.md` §1 (Ruling, 2026-07-25; API surface delivered by `docs/WP-054-Invoice-Workflow-Transition-Api-Decisions.md`; fixture gating corrected 2026-07-25) |
| **Per-Tenant Graph Configuration** — redesign `GraphOptions` from a single app-wide config to per-tenant data (`TenantGraphConfiguration` table in Azure SQL), multi-tenant Entra app registration with per-customer admin consent (client secret or certificate per tenant, certificate preferred), Key Vault secrets under a `graph-secret-{tenantId}` naming convention, and a per-tenant (or removed-from-shared-endpoint) health check. Owned by the Chief Technical Architect (design) / a Backend Engineer (implementation), as its own dedicated WP. | Before any second customer's mailbox is connected | **Blocking (future)** | `docs/WP-004-Graph-Multitenancy-Decision.md` |
| **Component tags on health check responses** — add a `"component"` field (`"graph"` / `"blob"` / `"database"`) to each entry in the `/health/ready` response body, so monitoring can filter/alert per component without a new endpoint. Small addition to `GraphMailboxHealthCheck`/`BlobStorageHealthCheck`. | Next touch of WP-004 or WP-005 | Low, small | `docs/WP-004-Health-Check-Severity-Decision.md` |
| **`IInvoiceRepository.GetBySupplierAsync`** — targeted, indexed supplier-scoped read, to replace the full in-memory `GetAllAsync()` scans currently used by `DuplicateDetectionService.CheckAsync` and `InvoiceProcessingService.ResolveSupplierAsync`. | When invoice volume makes `GetAllAsync()` costly | Low, deferred | `docs/WP-010-Duplicate-Flag-Persistence-Decision.md` (also noted independently in `docs/WP-012-Invoice-Processing-Pipeline-Decisions.md` item 6) |
| **Supplier fuzzy-matching / alias resolution** — `ResolveSupplierAsync`'s case-insensitive trimmed exact-match will create duplicate `Supplier` rows for OCR/extraction variance (e.g. "Acme Ltd" vs "Acme Ltd." vs "ACME LIMITED"). No fuzzy-matching scheme, alias table, or manual-merge capability exists. Approved for MVP as-is; this is a flagged gap, not a blocker — no scheme should be invented speculatively. | When supplier-name variance from OCR becomes a real data-quality problem | Low, deferred | `docs/WP-012-Invoice-Processing-Pipeline-Decisions.md` item 3 (Ruling, 2026-07-25) |
| **Reconcile `FixtureInvoiceClient` (WP-015) against WP-011's real DTO** — `GET /api/invoices` (search/status/sort/page, backed by `IInvoiceQueryService`) did not exist anywhere in `InvoicesController` until a 2026-07-25 repo-wide audit found the gap and added it; **that HTTP wiring was then removed again the same day** when WP-055's merge overwrote `InvoicesController.cs` wholesale (Chief Technical Architect ruling: WP-055's zip predates the WP-011 wiring and is authoritative for the controller — see `docs/WP-055-Invoice-Notes-Api-Decisions.md`). `IInvoiceQueryService`/`InvoiceQueryService`/`InvoiceListItemDto` (with `IsPotentialDuplicate`/`DuplicateCheckReason`) and the WP-052 Part D field-naming ruling (`SupplierInvoiceNumber`/`GrossTotal`/`Currency`) are all still intact and fully tested — only the controller's HTTP wiring needs to be re-added. **Still open:** re-wire `GET /api/invoices` into `InvoicesController`, then swap `FixtureInvoiceClient` for a real HTTP client — the latter is additionally blocked on the WP-002 row above. | Before WP-015's fixture client is replaced with a real HTTP-backed one | **Blocking (near-term)** | `docs/WP-015-Invoice-Queue-Decisions.md` item 1 (Ruling, 2026-07-25); field-naming direction per `docs/WP-052-Pipeline-And-Api-Hardening-Decisions.md` Part D (Ruling, 2026-07-25); endpoint delivered 2026-07-25, then removed 2026-07-25 by the WP-055 merge |
| **Reconcile `FixtureInvoiceDetailClient` (WP-016) against WP-008/WP-009/WP-011/WP-013's real shapes, and point `pdfUrl` at the real download endpoint** — all four dependencies are already deployed, including the proxied-download endpoint (`GET /api/invoices/{id}/download`, `docs/WP-052-Pipeline-And-Api-Hardening-Decisions.md` Part D). **Still open:** swapping `FixtureInvoiceDetailClient` for a real HTTP client (using the backend's field names, same ruling as the WP-015 row above) — blocked on the WP-002 row above. | Before WP-016's fixture client is replaced with a real HTTP-backed one | **Blocking (near-term)** | `docs/WP-016-Invoice-Review-Decisions.md` item 2 (Ruling, 2026-07-25); field-naming direction per `docs/WP-052-Pipeline-And-Api-Hardening-Decisions.md` Part D (Ruling, 2026-07-25) |
| **WP-056 — Persist per-field extraction confidence** — extend `Invoice`'s schema to persist Document Intelligence's per-field confidence scores past the original pipeline run, closing the gap `Invoice`'s own doc comment (WP-009) anticipated. Confirmed as genuinely new schema scope by WP-052 Part D, not something an API-endpoint-only task should silently add. | When a real requirement justifies the entity's growth (per WP-009's own doc comment) | Low, deferred | `docs/WP-052-Pipeline-And-Api-Hardening-Decisions.md` Part D (Ruling, 2026-07-25) |
| **WP-057 — Delete the `DUPLICATE_SUSPECTED` `StatusReference` row** — the status is confirmed unreachable in practice (zero transitions reference it in either template's confirmed graph, verified against the real database by WP-053), but the row itself has never been deleted. Requires a migration, not a code change. | Whenever a Backend Engineer picks up a general reference-data cleanup pass | Low, deferred | `docs/WP-053-Transition-Enforcement-Decisions.md` Discrepancy 2 (Ruling, 2026-07-25) |

## Closed

| Item | Closed by | Date |
|---|---|---|
| ~~`Invoice.IsPotentialDuplicate` / `Invoice.DuplicateCheckReason`~~ — persisted fields on `Invoice`, written by `InvoiceProcessingService.PersistDuplicateCheckResultAsync` via a new `IInvoiceRepository` dependency; `DuplicateDetectionService` unchanged (still pure compute). | This session's implementation - see `docs/WP-010-Duplicate-Flag-Persistence-Decision.md`'s "## Implementation" section | 2026-07-20 |
| ~~Invoice Notes API endpoint~~ — `GET`/`POST /api/invoices/{id}/notes` added to `InvoicesController`, exactly as ruled: separate resource (not folded into `InvoiceDetailResponse`); `InvoiceNote.AuthorDisplayName` (new column, migration `AddInvoiceNoteAuthorDisplayName`) resolved server-side from `ICurrentUserService.DisplayName`. **Superseded/reworked the same day by WP-055** (Chief Technical Architect ruling): `AuthorDisplayName` is now `nvarchar(200)` (not 256, the original implementation's column length) and `ICurrentUserService.DisplayName` is now a required interface member (not default-implemented as null — all ~10 test-double implementers were updated). `AddNoteAsync`/`GetNotesAsync` still return `InvoiceNoteDto` (id/content/author/timestamp). WP-017's `FixtureInvoiceNoteClient` swap itself remains open, blocked on the WP-002 row above. | This session's implementation, superseded/reworked by WP-055 - see `docs/WP-017-Invoice-Notes-Decisions.md` §1 (Ruling, 2026-07-25) and `docs/WP-055-Invoice-Notes-Api-Decisions.md` | 2026-07-25 |
